# 2026-09-16 Claudeランチャー開発記録

## 背景

claude.ai の特定チャット／プロジェクトをスマホのホーム画面から直接開きたい。
Chrome の「ホーム画面に追加」では claude.ai のトップ画面に飛んでしまう。

原因は claude.ai の PWA マニフェスト。`start_url: "/"` / `scope: "/"` のため、
ブラウザの「ホーム画面に追加」は表示中のURLではなくルートを起動する（仕様）。
→ URL をそのまま保持して開く自前アプリを作ることにした。

## 決めたこと

- アイコンは **アプリ内で追加・編集**（固定焼き込みではない）。チャットが増えても再ビルド不要
- ビルドは **PC のローカル SDK を直接叩く**（Gradle 不使用・ネット接続不要）
- 開き方は既定のインテント。項目ごとに「必ずブラウザで開く」を選べる
  （Claude アプリが claude.ai のリンクを横取りする場合の逃げ道）

## 成果物

`Desktop\右腕\プライベート\Claudeランチャー\`

- `ClaudeLauncher.apk` … 完成品（25KB・デバッグ自己署名）
- `build.bat` … ダブルクリックで再ビルド。結果は `build.log`
- `src/` `res/` `AndroidManifest.xml` … ソース
- `debug.keystore` … 署名鍵。**これを消すと更新インストールができなくなる**ので保持

パッケージ名 `com.rerise.claudelauncher` / minSdk 29 / targetSdk 35 / AndroidX 不使用。

## ビルド環境（このPCで確認済み）

- SDK: `%LOCALAPPDATA%\Android\Sdk`（build-tools 37.0.0 / platforms android-36.1）
- JDK: Android Studio 同梱 jbr（OpenJDK 21）
- 手順: aapt2 compile → aapt2 link → javac → d8 → dex を APK に投入 → zipalign → apksigner

## ハマった点

javac の `@argfile` にフルパスを書くと失敗する。
ユーザー名が日本語（`岸　優成`）で、`dir` は cp932、javac は argfile を UTF-8 として読むため。
→ ソースは **相対パスで直接引数に並べる**方式に変更。
　（`build.bat` の `SRC=` 行。.java を追加したらここにも追記する）

## 残タスク・今後

- スマホ側で実機確認（提供元不明アプリの許可が必要）
- 使うチャットの URL を登録
- 案2（ボタンを押すと裏で Claude が動くアプリ）は未着手

---

## 2026-09-18 追記（v2）

「新しいセッションを立てるたびに手入力なのはどうにかならないか」への対応。

まず調べた結論：**完全自動は不可**。claude.ai のチャット一覧を取れる公開APIが無く、
アプリ側はログインセッションも持たないため、一覧を勝手に同期することはできない。
（Anthropic の公開APIは claude.ai の会話とは別物）

代わりに入力経路を2つ足した。

- **共有メニューからの追加**: `AndroidManifest.xml` の MainActivity に
  `ACTION_SEND` / `text/plain` の intent-filter を追加。`launchMode="singleTask"` にして
  `onNewIntent` で受ける。EXTRA_TEXT から正規表現でURLを抜き、EXTRA_SUBJECT をタイトルに使う。
  タイトル末尾の「 - Claude」「 | Claude」等は落とす
- **クリップボード検知**: `onWindowFocusChanged(true)` で claude.ai のURLを拾い、
  未登録なら追加を提案。`dialogOpen` と `lastOfferedUrl` で二重表示を防ぐ

Android 10 以降バックグラウンドでのクリップボード読み取りは禁止なので、
検知はアプリがフォアグラウンドに来た瞬間のみ。

ビルドは同じ `build.bat` で通った（新規 .java は無いので SRC 行の変更も不要）。
署名鍵が同じなので上書きインストールで更新でき、登録済みの項目は残る。

---

## 2026-09-18 追記（v3）

「ホーム画面のアイコンから PWA で開けないか」への対応。

Chrome で claude.ai を「アプリをインストール」すると **WebAPK** という実体のある
パッケージ（`org.chromium.webapk.*`）になる。つまりパッケージ名で名指しでき、
`Intent.setPackage()` で開き先に指定できる。
しかもインテント経由なら manifest の start_url ではなく指定URLがそのまま開くため、
当初の「ホーム画面に追加するとトップに飛ぶ」問題は起きない。

実装:

- `Entry` に `pkg`（開き先パッケージ名）を追加。旧 `browser` フラグは pkg が空のときだけ効く後方互換として残す
- 編集画面のチェックボックスを「開き方」ピッカーに置き換え。
  `queryIntentActivities` で claude.ai を開けるアプリを実行時に列挙し、
  `org.chromium.webapk` で始まるものには「（PWA）」を付けて表示
- `OpenActivity` は pkg が入っていれば `setPackage`。失敗したら package を外して再試行

`AndroidManifest.xml` の `<queries>`（https の VIEW + BROWSABLE）が
Android 11 以降のパッケージ可視性のために必要。これは初版から入っていた。

---

## 2026-09-18 追記（v4 / v1.1）

アップデート機能と GitHub 配布を追加。

### アプリ側

- `Updater.java` を新規追加。`latest.json` を取得して versionCode を比較し、
  新しければダイアログ。ダウンロードはブラウザに投げる（インストールは Android 標準の流れ）
- ヘッダーに「更新確認」ボタン。加えて起動時に1日1回だけ黙って確認し、
  **新しい版があるときだけ**声をかける（`autoCheck`）
- `AndroidManifest.xml` に `INTERNET` 権限、versionCode=2 / versionName=1.1
- 新規 .java なので `build.bat` の `SRC=` 行に `Updater.java` を追記した

### 配布

- GitHub `koyomiyusei/claude-launcher`（**Public**）
  - アプリが認証なしで取りに行くため公開が必須。鍵も個人情報も入れていない
  - 既存の `uwan-board`（Private）+ `uwan-board-site`（Public）とは違い、1リポジトリにまとめた
- `latest.json` … versionCode / versionName / apkUrl / notes
- `.gitignore` で **`debug.keystore` を必ず除外**。これが漏れると誰でも更新APKに署名できてしまう
- `build.bat` の末尾に git push を組み込んだ。ビルドすればそのまま配布される

### ハマった点（未解決）

Claude の PC 操作（エクスプローラーのダブルクリック）で `push.bat` が起動しなかった。
`build.bat` は同じ操作で動いていたのに、途中から .bat の実行自体が反応しなくなった。
原因未特定。ダイアログは出ていない。結局この回は本人にダブルクリックしてもらう形にした。

### 配布の実現方法（2026-09-19 解決）

Windows 側でバッチを起動できない問題は最後まで直らなかったため、
**Cowork の Linux VM（device_bash）から直接 push する**形に切り替えた。

1. VM に GitHub CLI を導入（github.com からリリースを取得）
2. **OAuth デバイスフロー**で認証。本人がブラウザでコードを入力して承認するだけで、
   トークンは VM 内に保存され、Claude 側には渡らない
   - スコープは `repo read:org workflow` が必要。`repo` だけだと gh が保存を拒否する
   - device_bash は呼び出しごとに別シェルなので、**取得と保存は同一コールで行う**こと
3. `.git` は**マウント外**（VM の $HOME）に置き、`GIT_DIR` / `GIT_WORK_TREE` を指定する
   - マウント内に `.git` を作ると `index.lock` を消せず詰む（削除権限が要る）

以降は device_bash から `git push` で配布できる。Windows 側の操作は不要。
ただし gh の認証はセッション配下に保存されるため、新しいセッションでは再認証が要る見込み。

### 検証（2026-09-19）

- `https://raw.githubusercontent.com/koyomiyusei/claude-launcher/main/latest.json` → 200
- `.../ClaudeLauncher.apk` → 200 / 29,163 bytes / application/octet-stream

---

## 2026-09-22 追記（v1.2 / ビルドを GitHub Actions へ移行）

### なぜ移行したか

9/18 20:52 を最後に、PC で cmd.exe が起動しなくなった（原因未特定）。
切り分け結果：README.md のダブルクリックではメモ帳が開く＝ダブルクリック自体は正常。
一方 `.bat` / `.cmd` / cmd.exe を直接指定したショートカット、どれも起動しない。
ファイルではなく **cmd.exe の起動そのものが PC 側で止められている**。

→ ビルドを PC から切り離し、**GitHub Actions** で行う形に変更。以降 PC の操作は不要。

### 仕組み

- `build.sh` … build.bat と同じ手順の Linux 版。Actions の Ubuntu ランナーにある Android SDK を使う
- `.github/workflows/build.yml` … `src/` `res/` `AndroidManifest.xml` などが push されたら自動ビルド
  - 署名鍵は **GitHub Secret `DEBUG_KEYSTORE_B64`** から復元（リポジトリには入れない、ビルド後に削除）
  - ビルドした `ClaudeLauncher.apk` と `latest.json` を bot がリポジトリへ戻す（`[skip ci]` 付き）
  - `latest.json` のバージョンはマニフェストから自動で合わせる
- 署名証明書は初版と同一（SHA-256 `AE:92:08:F9:...`）。上書き更新でき、登録済みの項目は残る

### 更新の流れ（今後）

ソースを直して push → Actions がビルド → アプリの「更新確認」→ ダウンロード → 「更新しますか？」→ 完了

### v1.2 の中身

- **アプリ内更新**：PackageInstaller でダウンロードからインストールまでアプリが実行。
  初回のみ「この提供元のアプリを許可」が必要。OS の「更新しますか？」確認だけは省略不可（仕様）
- **開き方にPWAが出ない問題の修正**：`<queries>` にホスト名 `claude.ai` を追加。
  ホスト名なしだと claude.ai 限定の受け口（WebAPK）は Android 11+ のパッケージ可視性で見えない
- **更新確認ボタンを下部へ移動**（「＋ 追加」の右隣）
